name: Build UmaHelper EXE

on:
  push:
    branches: [ "main" ]
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies (pinned)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller==6.*

      # Simple sanity checks that work with paddleocr 2.7.3
      - name: Sanity check Paddle imports
        run: |
          python -c "import paddle; print('paddle:', paddle.__version__)"
          python -c "from paddleocr import PaddleOCR, __version__ as v; print('paddleocr:', v)"

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller main.py `
            --name UmaHelper `
            --onefile `
            --noconsole `
            --clean `
            --collect-all paddle `
            --collect-all paddleocr `
            --collect-all PySide6 `
            --hidden-import paddle `
            --hidden-import paddleocr `
            --add-data "assets;assets" `
            --add-data "roi;roi"

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: UmaHelper-Windows
          path: dist/UmaHelper.exe

      - name: Upload to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/UmaHelper.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
